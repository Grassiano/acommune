{
  "name": "acommune",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "shared",
    "relay",
    "mcp"
  ],
  "scripts": {
    "build": "tsc -b shared relay mcp",
    "clean": "tsc -b shared relay mcp --clean",
    "test": "npm run test --workspace acommune-shared && npm run test --workspace acommune-relay"
  },
  "devDependencies": {
    "@types/node": "26.0.0",
    "typescript": "5.6.3"
  }
}
