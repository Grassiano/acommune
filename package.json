{
  "name": "acommune",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "shared",
    "relay",
    "mcp",
    "cli"
  ],
  "scripts": {
    "build": "tsc -b shared relay mcp cli",
    "clean": "tsc -b shared relay mcp cli --clean",
    "test": "npm run test --workspace acommune-shared && npm run test --workspace acommune-relay && npm run test --workspace acommune"
  },
  "devDependencies": {
    "@types/node": "26.0.0",
    "typescript": "5.6.3"
  }
}
