{
  "name": "acommune",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "acommune": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test .test-dist/test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
